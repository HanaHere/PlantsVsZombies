.idea
extra resoursec


echo "# Compiled executables (NEVER commit these!)
*.exe
*.dll
*.o
*.obj

# IDE settings
.vscode/

# The ENTIRE Raylib library (this is why your push is failing - it's huge!)
RAYLIB/

# Local save data
savefile.dat

# Keep source code, ignore the compiled files inside any folders
extra resoursec/*.exe
sprites/*.exe" > .gitignore

READ.txt